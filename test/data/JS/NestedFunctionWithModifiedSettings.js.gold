  Thresholds = JAVA_SCRIPT:10,
function |outerFunction|(0)() {
  var myFunction = |function(age, name, isAdmin)|(1) {
    var result = false;
    var value = false;

    if (name === "Sarah") {
      if (age < 20 || age > 100) {
        if (isAdmin) {
          result = true;
          value = true;
        }
        else if (age == 42) {
          result = false;
        }
        else {
          result = true;
        }
      }
    }
    else if (name == "Gentry" && isAdmin) {
      result = false;
    }
    else {
      if (age == 50) {
        if (isAdmin) {
          if (name == "Amrit") {
            result = false;
          }
          else if (name == "Jane") {
            result = true;
          }
          else {
            result = false;
          }
        }
      }
    }

    return result;
  }
  myFunction(42, "Matt", true);
}

---------------------------------------------------------
(0): Cyclomatic Complexity Highlight: Property 'outerFunction' has cyclomatic complexity of 1 (10% of threshold)
(1): ReSharper Warning: Element has cyclomatic complexity of 14 (140% of threshold)

================
  Thresholds = JAVA_SCRIPT:21,
function |outerFunction|(0)() {
  var myFunction = |function(age, name, isAdmin)|(1) {
    var result = false;
    var value = false;

    if (name === "Sarah") {
      if (age < 20 || age > 100) {
        if (isAdmin) {
          result = true;
          value = true;
        }
        else if (age == 42) {
          result = false;
        }
        else {
          result = true;
        }
      }
    }
    else if (name == "Gentry" && isAdmin) {
      result = false;
    }
    else {
      if (age == 50) {
        if (isAdmin) {
          if (name == "Amrit") {
            result = false;
          }
          else if (name == "Jane") {
            result = true;
          }
          else {
            result = false;
          }
        }
      }
    }

    return result;
  }
  myFunction(42, "Matt", true);
}

---------------------------------------------------------
(0): Cyclomatic Complexity Highlight: Property 'outerFunction' has cyclomatic complexity of 1 (4% of threshold)
(1): Cyclomatic Complexity Highlight: Element has cyclomatic complexity of 14 (66% of threshold)

================
  Thresholds = JAVA_SCRIPT:30,
function |outerFunction|(0)() {
  var myFunction = |function(age, name, isAdmin)|(1) {
    var result = false;
    var value = false;

    if (name === "Sarah") {
      if (age < 20 || age > 100) {
        if (isAdmin) {
          result = true;
          value = true;
        }
        else if (age == 42) {
          result = false;
        }
        else {
          result = true;
        }
      }
    }
    else if (name == "Gentry" && isAdmin) {
      result = false;
    }
    else {
      if (age == 50) {
        if (isAdmin) {
          if (name == "Amrit") {
            result = false;
          }
          else if (name == "Jane") {
            result = true;
          }
          else {
            result = false;
          }
        }
      }
    }

    return result;
  }
  myFunction(42, "Matt", true);
}

---------------------------------------------------------
(0): Cyclomatic Complexity Highlight: Property 'outerFunction' has cyclomatic complexity of 1 (3% of threshold)
(1): Cyclomatic Complexity Highlight: Element has cyclomatic complexity of 14 (46% of threshold)
